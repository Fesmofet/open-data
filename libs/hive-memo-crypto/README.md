# hive-memo-crypto

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build hive-memo-crypto` to build the library.

## Running unit tests

Run `nx test hive-memo-crypto` to execute the unit tests via [Jest](https://jestjs.io).
