# hive-auth

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build hive-auth` to build the library.

## Running unit tests

Run `nx test hive-auth` to execute the unit tests via [Jest](https://jestjs.io).
