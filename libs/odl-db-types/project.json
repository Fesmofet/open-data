{
  "name": "odl-db-types",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/odl-db-types/src",
  "projectType": "library",
  "tags": ["type:lib", "scope:shared"],
  "// targets": "to see all targets run: nx show project odl-db-types --web",
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.lib.json",
        "cwd": "libs/odl-db-types"
      }
    }
  }
}
