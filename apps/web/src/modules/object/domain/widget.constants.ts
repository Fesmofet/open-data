/** Legacy widget column modes that open a link instead of embedding. */
export const WIDGET_COLUMN_NEW_TAB = 'newTab';
export const WIDGET_COLUMN_FORWARD = 'forward';

/** Primary tab segment for widget host objects. */
export const WIDGET_PRIMARY_TAB_SEGMENT = 'widget';

/** Legacy widget embed type that uses iframe srcDoc. */
export const WIDGET_EMBED_TYPE_WIDGET = 'Widget';
