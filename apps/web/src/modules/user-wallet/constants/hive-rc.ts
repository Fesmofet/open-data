/**
 * Minimum RC the delegator must keep on account (~Hive account creation fee in RC units).
 * Used by delegate_rc max amount; tune against staging if chain globals shift.
 */
export const HIVE_RC_DELEGATOR_RESERVE = 3_000_000_000;
